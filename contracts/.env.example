# Sepolia deployment (optional — for npm run deploy:sepolia)
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
SEPOLIA_PRIVATE_KEY=0x...

# Local Hardhat node uses hardhat.config.ts defaults (no .env required for localhost)
